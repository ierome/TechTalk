import React from 'react'

export default props => {

    return (
        <div id="entry">
        <textarea type="text" id="entryInput" placeholder="Enter a message..."></textarea>
        <button id="sendButton">Send</button>
    </div>
    )
}